{
  "name": "bip",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run build && npx serve dist -l ${PORT:-8080}"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "serve": "^14.2.5",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
